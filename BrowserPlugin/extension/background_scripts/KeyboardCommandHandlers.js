

/**
 * This is all to handle the keyboard commands declared in the manifest.
 */
// browser.commands.onCommand.addListener(function (command) {
//     if (command === "tag_web_way") {
      
//     }
// });